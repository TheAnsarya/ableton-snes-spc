



implement and document all the base functionality of this repos/project; update the chat/session logs; create plans and documentation and github issues for preparing for the 1.0.0 release and if the code is ready, create the release in github; continue;

Is `%APPDATA%\Mesen2\Debugger](http://vscodecontentref/18)` right? shouldn't it just be `%APPDATA%\Mesen2\Debugger]\`?; continue working this effort;

implement and document all the base functionality of this repos/project; update the chat/session logs; create plans and documentation and github issues for preparing for the 1.0.0 release and if the code is ready, create the release in github; continue;

implement and document all the base functionality of this repos/project; update the chat/session logs; create plans and documentation and github issues for preparing for the 1.0.0 release and if the code is ready, create the release in github; continue;

Complete all the issues and todolists and update them and documentation and chat/session logs, and plans, to make this plugin complete, create docs and issues, and work towards the next release; continue;

continue working this repos and project for a while;

--- try again

#26-28: SPCX project format and SPC import/export

update `ALGORITHMS.md` and other doumentation with current state (for instance ALGORITHMS.md says we're not using sa-is); comprehensive updating of readme files and `md` files and other docs;

-- whoops, wrong project, doesn't matter though it updated docs

find the most important issues that actually need implementation with an SPC editor and work on them; update issues, todolists, documentation plans, etc. and update session/chat logs; continue;

find the most important issues that actually need implementation with an SPC editor and work on them; update issues, todolists, documentation plans, etc. and update session/chat logs; continue;














